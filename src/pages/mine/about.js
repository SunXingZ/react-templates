import React from "react";
import { AppContainer } from "components";

const About = (props) => {
  return (
    <AppContainer>
      <p style={{
        height: 800,
        textAlign: "center"
      }}>about content</p>
    </AppContainer>
  )
}

export default About;