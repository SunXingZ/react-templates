import React from "react";
import { AppContainer } from "components";

const Home = (props) => {
  return (
    <AppContainer>
      <p style={{
        height: 800,
        textAlign: "center"
      }}>home content</p>
    </AppContainer>
  )
}

export default Home;