/**
 * 这里可以配置应用主题色和各组件全局配置
 * 具体参考：react-native-elements文档
 */

const AppTheme = {
	colors: {
		primary: '#FF8C00'
	},
	Header: {
		barStyle: 'light-content',
		statusBarProps: {
			translucent: true,
			backgroundColor: 'rgba(0,0,0,0)'
		}
	},
	Button: {
		titleStyle: {
			fontSize: 16
		}
	}
};

export default AppTheme;
